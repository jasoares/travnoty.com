!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %title= content_for?(:title) ? "#{yield(:title)} · Travnoty" : "Travnoty - Your Travian notifier"
    %meta{:content => "Get notifications from your travian account every time you are being attacked or when your wharehouse is full.", :name => "description"}
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    = yield(:head)
  %body
    %header#header
      .container.clearfix
        = link_to(root_url, :class => 'travnoty-logo') do
          = image_tag 'travnoty_logo_2_small.png', size: '128x40'
        %nav#nav
          = render 'layouts/navigation' if current_user
        = render 'layouts/profile'
    #content
      .small-container
        = render 'layouts/messages'
      = yield
    %footer#footer.container
      = render 'layouts/footer'