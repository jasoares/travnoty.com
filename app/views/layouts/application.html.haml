!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %title= content_for?(:title) ? "#{yield(:title)} · Travnoty" : "Travnoty - Get notifications from your Travian account."
    %meta{:content => "Get notifications from your travian account every time you are being attacked or when your wharehouse is full.", :name => "description"}
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    = yield(:head)
  %body{ :class => signed_in? ? "signed_in" : "signed_out" }
    %header#header
      = render 'layouts/header'
    #flash.container
      = render 'layouts/messages'
    #container
      = yield
    %footer#footer.container
      = render 'layouts/footer'