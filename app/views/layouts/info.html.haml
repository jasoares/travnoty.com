!!!
%html
  %head
    %meta{ charset: 'utf-8' }
    %meta{ content: 'width=device-width, initial-scale=1.0', name: 'viewport' }
    %title= content_for?(:title) ? "#{yield(:title)} · Travnoty" : "Travnoty - Travian Servers Information"
    %meta{ content: 'Always know when new Travian servers start, which servers started recently and how long will they last', name: 'description' }
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    = render 'shared/header'
    #content
      = yield
    = render 'shared/footer'